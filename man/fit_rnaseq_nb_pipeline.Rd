% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rnaseq_nb_pipeline.R
\name{fit_rnaseq_nb_pipeline}
\alias{fit_rnaseq_nb_pipeline}
\title{Fit a Bayesian Hierarchical Negative Binomial Mixed Model for RNA-Seq Counts}
\usage{
fit_rnaseq_nb_pipeline(
  data,
  gene_var,
  count_var,
  condition_var,
  covariates = NULL,
  subject_var = NULL,
  sample_var,
  offset_var = NULL,
  chains = 4,
  iter = 2000,
  warmup = 1000,
  cores = 1,
  seed = NA,
  cache_file = NULL,
  ...
)
}
\arguments{
\item{data}{A long-format data.frame: one row per gene x sample
combination.}

\item{gene_var}{Character. Column identifying the gene. Gets a
gene-level random intercept and random slope for \code{condition_var}.}

\item{count_var}{Character. Column of raw (unnormalized) RNA-seq counts.}

\item{condition_var}{Character. The fixed effect of primary biological
interest (e.g. treatment arm, genotype). Given a gene-level random
slope so per-gene effects are partially pooled toward the population
average. Should be numeric or a two-level factor: the shrinkage
summary and plot currently reflect only the first non-reference
coefficient, so a factor with more than two levels will emit a warning
and only show one contrast.}

\item{covariates}{Character vector of additional fixed effects to adjust
for (population-level only -- no gene-specific slope). Default \code{NULL}.}

\item{subject_var}{Optional character. Subject/individual identifier for
repeated-measures or longitudinal designs; gets a subject-level random
intercept. Default \code{NULL} (no repeated-measures structure).}

\item{sample_var}{Character. Column identifying the sample -- used to
compute the library-size offset when \code{offset_var} is not supplied.}

\item{offset_var}{Optional character. Column of pre-computed
normalization factors (raw scale, e.g. total counts or a DESeq2/edgeR
size factor) to use as the model offset. If \code{NULL} (the default), a
simple total-count-per-sample offset is computed internally -- adequate
for a first pass, but a proper size-factor normalization (e.g.
median-of-ratios) is recommended for a publication-grade analysis and
can be supplied here instead.}

\item{chains, iter, warmup, cores}{Passed to \code{\link[brms:brm]{brms::brm()}}. Defaults
(\verb{chains = 4, iter = 2000, warmup = 1000, cores = 1}) are a reasonable
starting point for exploration; increase \code{cores} to \code{chains} if your
machine allows running them in parallel, and increase \code{iter} if the
convergence verdict flags a low effective-sample-size ratio.}

\item{seed}{Optional integer seed passed to \code{\link[brms:brm]{brms::brm()}}. Default \code{NA}
(brms's own default -- no fixed seed). Note this must be \code{NA}, not
\code{NULL}, since \code{brms::brm()} cannot coerce \code{NULL} to a numeric value.}

\item{cache_file}{Optional path. If it exists, the cached fit is loaded
via \code{\link[=readRDS]{readRDS()}} and no model is refit. If it does not exist, the model
is fit and then saved there via \code{\link[=saveRDS]{saveRDS()}}. Recommended for any model
that takes more than a minute or two to fit, since Stan model
compilation and sampling both restart from scratch otherwise.}

\item{...}{Additional arguments passed through to \code{\link[brms:brm]{brms::brm()}}.}
}
\value{
An object of class \code{"rnaseq_nb_pipeline"}, a list with elements:
\describe{
\item{model}{The fitted \code{brmsfit} object.}
\item{convergence}{A list with \code{max_rhat}, \code{min_neff_ratio}, and the
full per-parameter summaries.}
\item{shrinkage}{A data.frame with one row per gene: the partially
pooled (population + gene-level deviation) estimate of the
\code{condition_var} effect, its credible interval, and the population-level
estimate for comparison.}
\item{dispersion}{A list with the posterior summary of the negative
binomial shape parameter.}
\item{plots}{A named list of \code{ggplot} objects: \code{pp_check},
\code{shrinkage_plot}, and \code{rhat_plot}.}
\item{verdicts}{A data.frame summarizing convergence, posterior
predictive fit, and dispersion, in the same style as
\code{\link[=fit_coxph_pipeline]{fit_coxph_pipeline()}}.}
}
}
\description{
Fits a multilevel negative binomial model to long-format RNA-seq count
data (one row per gene x sample) via \code{\link[brms:brm]{brms::brm()}}. The effect of primary
biological interest (\code{condition_var}) gets a gene-level random slope, so
genes borrow statistical strength from one another (partial pooling)
rather than being tested one at a time in isolation. An optional
subject-level random intercept accounts for repeated-measures /
longitudinal correlation within individuals.
}
\details{
\code{brms} (and the Stan toolchain it depends on) is listed as a \code{Suggests},
not a hard dependency of \code{omicsuite}, since it requires a working C++
compiler. Install it with \code{install.packages("brms")} before calling this
function.
}
\examples{
\donttest{
if (requireNamespace("brms", quietly = TRUE)) {
  set.seed(1)
  n_genes <- 8
  n_samples <- 12
  dat <- expand.grid(gene = paste0("gene_", seq_len(n_genes)),
                      sample = paste0("sample_", seq_len(n_samples)))
  dat$condition <- factor(rep(rep(c("control", "treated"), each = n_samples / 2),
                               n_genes))
  dat$count <- stats::rnbinom(nrow(dat), mu = 50, size = 5)

  fit <- fit_rnaseq_nb_pipeline(
    data = dat, gene_var = "gene", count_var = "count",
    condition_var = "condition", sample_var = "sample",
    chains = 1, iter = 200, warmup = 100
  )
  print(fit)
}
}

}
