% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coxph_pipeline.R
\name{fit_coxph_pipeline}
\alias{fit_coxph_pipeline}
\title{Fit a Cox Proportional Hazards Pipeline with Full Diagnostics}
\usage{
fit_coxph_pipeline(
  data,
  time_var,
  event_var,
  covariates,
  adjust_for = NULL,
  id_var = NULL,
  frailty = FALSE,
  alpha = 0.05,
  influence_cutoff_sd = 1
)
}
\arguments{
\item{data}{A data.frame containing the survival data.}

\item{time_var}{Character. Name of the time-to-event column.}

\item{event_var}{Character. Name of the event indicator column
(1 = event occurred, 0 = censored).}

\item{covariates}{Character vector of covariate names for the primary
model of interest.}

\item{adjust_for}{Optional character vector of additional covariates to
adjust for. When supplied, both an unadjusted model (\code{covariates} only)
and an adjusted model (\code{covariates} + \code{adjust_for}) are fit, so you can
see how much the estimates of interest move.}

\item{id_var}{Optional character. Cluster/subject identifier, for
robust sandwich variance (\code{cluster()}) or a shared frailty term.}

\item{frailty}{Logical. If \code{TRUE} and \code{id_var} is supplied, fits a gamma
frailty term instead of a robust cluster-adjusted variance. Default
\code{FALSE}.}

\item{alpha}{Significance threshold used to flag diagnostic checks.
Default \code{0.05}.}

\item{influence_cutoff_sd}{Numeric. Observations with a scaled dfbeta
beyond this many "typical" units (\code{2 / sqrt(n)} scaled by this factor)
are flagged as influential. Default \code{1} (i.e. the standard \code{2/sqrt(n)}
cutoff).}
}
\value{
An object of class \code{"coxph_pipeline"}, a list with elements:
\describe{
\item{model_unadjusted}{The unadjusted \code{coxph} fit.}
\item{model_adjusted}{The adjusted \code{coxph} fit, or \code{NULL} if \code{adjust_for}
was not supplied.}
\item{primary_model}{Whichever of the two above is the model of record
(adjusted if available, else unadjusted) -- used for diagnostics.}
\item{ph_test}{The \code{\link[survival:cox.zph]{survival::cox.zph()}} result for \code{primary_model}.}
\item{influence}{A list with the dfbeta/dfbetas matrices (indexed to
\code{used_rows}, in fitted-model order), \code{flagged_rows} (indices into the
original \code{data} you passed in -- so \code{data[fit$influence$flagged_rows, ]}
always works, even if \code{coxph()} silently dropped rows with missing
values), \code{used_rows} (which original rows the model was actually
fitted on), and \code{n_used}.}
\item{functional_form}{A named list of data.frames (martingale residual
vs. covariate value) for each continuous covariate in \code{covariates}.}
\item{plots}{A named list of \code{ggplot} objects: \code{ph_plot}, \code{influence_plot},
and one \verb{functional_form_<var>} entry per continuous covariate.}
\item{verdicts}{A data.frame summarizing every diagnostic check, its
pass/flag status, and a short interpretive note.}
\item{alpha}{The significance threshold used.}
}
}
\description{
Fits a Cox proportional hazards model and runs the diagnostic suite you'd
want in a methods section before trusting the hazard ratios: an
unadjusted-versus-adjusted comparison, global and covariate-level
proportional hazards testing, influence diagnostics, and functional form
checks for continuous covariates via martingale residuals.
}
\examples{
\donttest{
set.seed(1)
n <- 200
dat <- data.frame(
  time = rexp(n, rate = 0.05),
  event = rbinom(n, 1, 0.7),
  age = rnorm(n, 55, 10),
  arm = factor(sample(c("control", "treatment"), n, replace = TRUE))
)
fit <- fit_coxph_pipeline(
  data = dat, time_var = "time", event_var = "event",
  covariates = "arm", adjust_for = "age"
)
print(fit)
}

}
