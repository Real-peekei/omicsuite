% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gillespie_epidemic.R
\name{simulate_gillespie_epidemic}
\alias{simulate_gillespie_epidemic}
\title{Simulate a Stochastic SIR or SEIR Epidemic via the Gillespie Algorithm}
\usage{
simulate_gillespie_epidemic(
  model = c("SIR", "SEIR"),
  initial_state,
  params,
  t_max,
  n_sim = 100,
  seed = NULL,
  grid_points = 200,
  extinction_threshold = 0.05
)
}
\arguments{
\item{model}{Character. Either \code{"SIR"} or \code{"SEIR"}.}

\item{initial_state}{Named numeric vector of starting compartment counts.
Must include \code{S}, \code{I}, \code{R} for \code{"SIR"}, or \code{S}, \code{E}, \code{I}, \code{R} for
\code{"SEIR"}.}

\item{params}{Named list of rate parameters. Must include \code{beta}
(transmission rate) and \code{gamma} (recovery rate) for \code{"SIR"}; \code{"SEIR"}
additionally requires \code{sigma} (the rate of progression from exposed to
infectious, i.e. \code{1 / sigma} is the mean incubation period).}

\item{t_max}{Numeric. Maximum simulation time.}

\item{n_sim}{Integer. Number of independent stochastic realizations to
run. Default \code{100}, enough to characterize the extinction probability
and the envelope around the median trajectory without being slow.}

\item{seed}{Optional integer seed for reproducibility.}

\item{grid_points}{Integer. Number of time points used when interpolating
realizations onto a common grid for the summary envelope and plots.
Default \code{200}.}

\item{extinction_threshold}{Numeric in \verb{(0, 1)}. A realization is
classified as an early stochastic fadeout if its final outbreak size
(total ever infected) is below this fraction of the total population.
Default \code{0.05}.}
}
\value{
An object of class \code{"gillespie_epidemic"}, a list with elements:
\describe{
\item{simulations}{A list of \code{n_sim} data.frames, one per realization,
each with columns \code{time} and one column per compartment.}
\item{summary}{A named list (one entry per compartment) of data.frames
with \code{time}, \code{median}, \code{lower} (5th percentile), and \code{upper} (95th
percentile) across realizations, on a common time grid.}
\item{final_size, peak_time, peak_size}{Numeric vectors, one entry per
realization: total ever infected, time of peak infectious count, and
peak infectious count.}
\item{r0}{The basic reproduction number implied by \code{params}
(\code{beta / gamma}, the same threshold quantity for both SIR and SEIR).}
\item{prop_extinct}{Proportion of realizations classified as an early
stochastic fadeout.}
\item{plots}{A named list of \code{ggplot} objects: \code{trajectory_plot},
\code{final_size_hist}, \code{peak_time_hist}.}
\item{verdicts}{A data.frame summarizing the basic reproduction number
and the extinction probability, in the same style as
\code{\link[=fit_coxph_pipeline]{fit_coxph_pipeline()}}.}
}
}
\description{
Runs one or more exact stochastic realizations of a compartmental epidemic
model (SIR or SEIR) using Gillespie's direct method (the stochastic
simulation algorithm), then summarizes the ensemble: a median trajectory
with a 5-95\% envelope, the distribution of final outbreak size and peak
timing across realizations, the basic reproduction number implied by the
rate parameters, and the proportion of realizations that fade out early
by chance rather than becoming a sustained outbreak -- a distinction a
deterministic ODE model can't show you at all.
}
\examples{
sir_fit <- simulate_gillespie_epidemic(
  model = "SIR",
  initial_state = c(S = 999, I = 1, R = 0),
  params = list(beta = 0.4, gamma = 0.1),
  t_max = 100, n_sim = 50, seed = 1
)
print(sir_fit)

seir_fit <- simulate_gillespie_epidemic(
  model = "SEIR",
  initial_state = c(S = 999, E = 0, I = 1, R = 0),
  params = list(beta = 0.4, sigma = 0.2, gamma = 0.1),
  t_max = 150, n_sim = 50, seed = 1
)
print(seir_fit)

}
