% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/competing_risks_pipeline.R
\name{fit_competing_risks_pipeline}
\alias{fit_competing_risks_pipeline}
\title{Fit a Competing Risks Pipeline}
\usage{
fit_competing_risks_pipeline(
  data,
  time_var,
  event_var,
  failcode = 1,
  cencode = 0,
  group_var = NULL,
  covariates = NULL,
  alpha = 0.05
)
}
\arguments{
\item{data}{A data.frame containing the survival data.}

\item{time_var}{Character. Name of the time-to-event column.}

\item{event_var}{Character. Name of the event-type column (see Details
above for its encoding -- not the same as \code{event_var} elsewhere in
\code{omicsuite}).}

\item{failcode}{The event code (as it appears in \code{data[[event_var]]})
representing the event of primary interest. Default \code{1}.}

\item{cencode}{The event code representing censoring. Default \code{0}.}

\item{group_var}{Optional character. Grouping variable for separate CIF
curves and Gray's test. If \code{NULL} (the default), a single CIF per
event type is estimated with no group comparison.}

\item{covariates}{Optional character vector of covariate names for the
Fine-Gray model (\code{cov1} in \code{\link[cmprsk:crr]{cmprsk::crr()}}). Factors are dummy-coded
automatically via \code{\link[stats:model.matrix]{stats::model.matrix()}}. If \code{NULL} (the default),
only the CIF/Gray's-test part of the pipeline runs -- no subdistribution
hazard model is fit.}

\item{alpha}{Significance threshold for Gray's test and the Fine-Gray
model's verdicts. Default \code{0.05}.}
}
\value{
An object of class \code{"competing_risks_pipeline"}, a list with
elements:
\describe{
\item{cuminc_fit}{The \code{cuminc} object from \code{\link[cmprsk:cuminc]{cmprsk::cuminc()}}.}
\item{crr_fit}{The \code{crr} object from \code{\link[cmprsk:crr]{cmprsk::crr()}}, or \code{NULL} if
\code{covariates} was not supplied.}
\item{cif_summary}{A long-format data.frame: time, cumulative
incidence estimate, event type, and group (if \code{group_var} was
supplied) -- the tidy form of \code{cuminc_fit} used for plotting.}
\item{event_summary}{A data.frame: counts and proportions for each
event code observed in \code{data[[event_var]]}, including censoring.}
\item{plots}{A named list of \code{ggplot} objects: \code{cif_plot}, carrying its
matching verdict notes as a wrapped caption.}
\item{verdicts}{A data.frame: event-type breakdown as \code{"info"}, Gray's
test as \code{"interpretation"} (when \code{group_var} is supplied), and
subdistribution hazard ratios as \code{"interpretation"} per covariate
term (when \code{covariates} is supplied).}
}
}
\description{
Fits nonparametric cumulative incidence functions (CIFs) for each event
type, optionally compares them across groups with Gray's test, and fits
a Fine-Gray subdistribution hazard model for the event type of primary
interest via \code{\link[cmprsk:crr]{cmprsk::crr()}}.
}
\details{
\strong{\code{event_var} means something different here than in \code{\link[=fit_km_pipeline]{fit_km_pipeline()}}
or \code{\link[=fit_coxph_pipeline]{fit_coxph_pipeline()}}.} Those treat \code{event_var} as a binary
indicator (1 = event, 0 = censored). Here it must encode \emph{which} event
type occurred: \code{0} = censored (or whatever \code{cencode} is set to), \code{1} =
the event of primary interest, \code{2}, \code{3}, ... = competing event types
that preclude the event of interest from ever happening (e.g. death from
another cause when the event of interest is relapse). Reusing a plain
0/1 binary indicator here silently treats every non-event as a
competing risk of type... there is none, which just reduces to the
single-event case -- not wrong, but usually not what you meant to ask.

The reason a dedicated model matters: fitting a standard Cox model that
treats competing events as ordinary censoring overestimates the event of
interest's cumulative incidence, because it implicitly assumes subjects
who experienced a competing event would have gone on to experience the
event of interest had they lived. The Fine-Gray model avoids that
assumption by keeping competing-event subjects in the risk set on the
subdistribution timescale instead of removing them.
}
\examples{
\donttest{
if (requireNamespace("cmprsk", quietly = TRUE)) {
  set.seed(1)
  n <- 300
  dat <- data.frame(
    time = rexp(n, rate = 0.1),
    # 0 = censored, 1 = event of interest, 2 = competing event
    event = sample(c(0, 1, 2), n, replace = TRUE, prob = c(0.3, 0.5, 0.2)),
    arm = factor(sample(c("control", "treatment"), n, replace = TRUE))
  )
  fit <- fit_competing_risks_pipeline(
    dat, "time", "event", group_var = "arm", covariates = "arm"
  )
  print(fit)
}
}

}
