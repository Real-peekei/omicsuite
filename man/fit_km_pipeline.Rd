% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/km_pipeline.R
\name{fit_km_pipeline}
\alias{fit_km_pipeline}
\title{Fit a Kaplan-Meier Survival Pipeline}
\usage{
fit_km_pipeline(
  data,
  time_var,
  event_var,
  strata_var = NULL,
  parametric_dist = NULL,
  conf_type = "log-log",
  alpha = 0.05
)
}
\arguments{
\item{data}{A data.frame containing the survival data.}

\item{time_var}{Character. Name of the time-to-event column.}

\item{event_var}{Character. Name of the event indicator column
(1 = event occurred, 0 = censored).}

\item{strata_var}{Optional character. Name of a grouping variable for
separate KM curves and a log-rank test. If \code{NULL} (the default), a
single overall curve is fit with no group comparison.}

\item{parametric_dist}{Optional character naming a distribution accepted
by \code{\link[flexsurv:flexsurvreg]{flexsurv::flexsurvreg()}}'s \code{dist} argument (e.g. \code{"weibull"},
\code{"exponential"}, \code{"gompertz"}, \code{"gengamma"}). If supplied, fits a
parametric model of that family and overlays it on the KM curve.}

\item{conf_type}{Confidence interval type passed to
\code{\link[survival:survfit]{survival::survfit()}}'s \code{conf.type}. Default \code{"log-log"} (bounded to
\verb{[0, 1]}, generally preferred over the plain \code{"plain"} linear CI).}

\item{alpha}{Significance threshold for the log-rank test verdict.
Default \code{0.05}.}
}
\value{
An object of class \code{"km_pipeline"}, a list with elements:
\describe{
\item{fit}{The \code{survfit} object.}
\item{parametric_fit}{The \code{flexsurvreg} object, or \code{NULL} if
\code{parametric_dist} was not supplied.}
\item{median_survival}{A data.frame: one row per stratum (or one row
named \code{"Overall"} if \code{strata_var} was not supplied), with median
survival time and its confidence interval.}
\item{logrank_test}{The \code{survdiff} object, or \code{NULL} if \code{strata_var}
was not supplied.}
\item{plots}{A named list of \code{ggplot}-family objects: \code{km_plot}
(a \code{ggsurvplot} object if \code{survminer} is installed, otherwise a plain
\code{ggplot}), and \code{parametric_overlay} if \code{parametric_dist} was supplied
and the overlay could be built (see Details).}
\item{verdicts}{A data.frame: median survival per stratum and the
log-rank test as \code{"interpretation"} rows, censoring summary and
parametric-fit AIC as \code{"info"} rows, and the parametric overlay as a
\code{"review"} row.}
}
}
\description{
Fits a nonparametric Kaplan-Meier survival curve (optionally stratified
by a grouping variable), runs a log-rank test for group differences, and
reports median survival time with its confidence interval per stratum --
the descriptive complement to \code{\link[=fit_coxph_pipeline]{fit_coxph_pipeline()}}'s modeling of
covariate effects. Optionally overlays a parametric fit (via
\code{\link[flexsurv:flexsurvreg]{flexsurv::flexsurvreg()}}) for a visual and AIC-based check of whether a
parametric distribution (Weibull, exponential, etc.) is a reasonable
summary of the same data.
}
\details{
\code{survminer} and \code{flexsurv} are \code{Suggests}, not hard dependencies (see
\code{CONTRIBUTING.md}'s dependency policy). Without \code{survminer} installed,
the KM plot falls back to a plain \code{ggplot2} step-function plot (no risk
table). \code{flexsurv} is only required when \code{parametric_dist} is supplied.
If the parametric overlay plot can't be built (e.g. an unexpected
\code{summary.flexsurvreg()} output shape on some \code{flexsurv} version), it's
silently omitted from \code{plots} with a warning rather than raising an
error -- \code{parametric_fit} and its AIC verdict are unaffected either way.
}
\examples{
set.seed(1)
n <- 200
dat <- data.frame(
  time = rexp(n, rate = 0.05),
  event = rbinom(n, 1, 0.7),
  arm = factor(sample(c("control", "treatment"), n, replace = TRUE))
)
fit <- fit_km_pipeline(dat, "time", "event", strata_var = "arm")
print(fit)

}
